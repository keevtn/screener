# StockTwitsExtractor

**Anchor:** `backend/UnstructuredModule.py:116`

**Purpose:** StockTwits symbol-stream poller, disabled by default (Cloudflare TLS fingerprinting blocks plain aiohttp). Listed for completeness.

**Receives from:** entry point — external feed.

*Stage: 01 Ingest*

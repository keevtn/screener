# start_ps1

**Anchor:** `start.ps1`

**Purpose:** Windows launcher: three supervised windows (-Trader / -FinBERT / -Stop) that run the pipeline, trader driver and firehose locally.

**Feeds:** [[run_pipeline.main|main]] — launches the pipeline window.

**Feeds:** [[run_trader_driver]] — launches the trader window.

**Feeds:** [[BlueskyFirehose]] — launches the firehose window.

*Stage: 13 Ops*
